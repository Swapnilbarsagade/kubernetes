apiVersion: v1
kind: Service
metadata:
  name: todolb
spec:
  selector:
    app: todo
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: tomcat
