apiVersion: v1 
kind: Service
metadata:
        name: todolb 
spec:
    selector:
          app: todo
    type: LoadBalancer
    ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: nginx