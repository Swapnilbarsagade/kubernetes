apiVersion: v1
kind: Service
metadata:
  name: netflixclone
  namespace: clone

spec:
  type: LoadBalancer
  selector:
    app:  netflixclone
  ports:
  - port: 8081
    targetPort: 8080
    name: netflixclone
    protocol: TCP